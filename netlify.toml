[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: every route is served by the React app (index.html), which then
# renders the matching captured page. Real files under /site/* are served first.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
